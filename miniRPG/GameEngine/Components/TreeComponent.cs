namespace miniRPG.GameEngine.Components;

public class TreeComponent(int variation, int maxHealth)
{
    public int Variation = variation;
    public int Health = maxHealth;
    public int MaxHealth = maxHealth;
}